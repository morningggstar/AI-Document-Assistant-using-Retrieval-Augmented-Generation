# AI-Document-Assistant-using-Retrieval-Augmented-Generation
An AI-powered application that allows users to query documents using natural language. The system implements a Retrieval-Augmented Generation (RAG) pipeline to retrieve relevant information from documents and generate context-aware responses using a Large Language Model (LLM).

This project demonstrates how modern AI applications combine vector search, embeddings, and LLMs to build intelligent document assistants.

## Features
* Upload and process documents
* Automatic text chunking and preprocessing
* Vector embeddings for semantic search
* Retrieval of relevant document segments
* LLM-based answer generation
* Natural language question answering
